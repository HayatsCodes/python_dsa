# static arrays are fixed size
# reading and writing values to an array is O(1)
# writing to the endof an array is O(1) if available
# writing to any other spot in the array is inefficient and it's O(n)
